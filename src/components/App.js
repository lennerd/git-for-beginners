import React, { PureComponent } from 'react';

import Scene from './Scene';

class App extends PureComponent {
  render() {
    return (
      <div>
        <Scene />
      </div>
    );
  }
}

export default App;
